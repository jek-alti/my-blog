# CLAUDE.md

이 파일은 이 저장소에서 작업하는 Claude Code에게 프로젝트의 목적, 구조, 규칙을 안내합니다.

## 프로젝트 개요

마크다운(`.md`) 파일을 읽어 정적 블로그 웹사이트로 변환하는 프로젝트입니다.

- **프레임워크 없음**: React, Vue, Next.js 등 프레임워크나 빌드 툴 체인을 사용하지 않습니다. 순수 **HTML, CSS, JavaScript(바닐라)** 로만 구현합니다.
- **정적 사이트**: 서버 로직 없이 브라우저에서 동작하며, 어떤 정적 호스팅(GitHub Pages, Netlify 등)에도 그대로 올릴 수 있어야 합니다.
- **의존성 최소화**: 외부 라이브러리는 꼭 필요할 때만 사용합니다. 마크다운 파싱처럼 직접 구현이 번거로운 부분은 CDN으로 가벼운 단일 라이브러리(예: marked, highlight.js)를 불러오는 것을 허용합니다. npm 설치·번들링은 도입하지 않습니다.

## 디자인 원칙

- **깔끔하고 읽기 좋은 타이포그래피**: 본문은 넉넉한 줄 간격(line-height 약 1.7), 적당한 본문 폭(약 65~75자, `max-width` 기준 대략 680~720px)을 유지합니다. 읽기 경험이 최우선입니다.
- **다크 모드 지원**: CSS 커스텀 프로퍼티(변수)로 라이트/다크 팔레트를 정의합니다.
  - 시스템 설정을 `prefers-color-scheme`로 자동 반영하고,
  - 사용자가 직접 토글할 수 있는 버튼을 제공하며, 선택값은 `localStorage`에 저장해 재방문 시 유지합니다.
  - 토글 버튼은 라이트/다크 각각에서 명도 대비(WCAG AA 수준)를 확보합니다.
- **모바일 우선 반응형**: 모바일에서 먼저 잘 보이도록 설계하고, 미디어 쿼리로 넓은 화면을 확장합니다. 고정 폭·가로 스크롤을 만들지 않습니다. 이미지·코드 블록·표는 화면을 넘치지 않도록 처리합니다.
- **접근성**: 의미에 맞는 HTML 태그(`<article>`, `<nav>`, `<main>`, `<time>` 등)를 사용하고, 색상만으로 정보를 전달하지 않습니다.

## 제안 디렉토리 구조

```
my-blog/
├── index.html          # 글 목록(홈)
├── post.html           # 개별 글 렌더링 페이지 (?slug=... 로 글 지정)
├── css/
│   └── style.css       # 전역 스타일 + CSS 변수(라이트/다크 팔레트)
├── js/
│   ├── main.js         # 목록 렌더링, 라우팅, 공통 로직
│   ├── post.js         # 마크다운 로드 → HTML 변환 → 렌더링
│   └── theme.js        # 다크모드 토글 + localStorage 저장/복원
├── posts/
│   ├── posts.json      # 글 메타데이터 목록(제목, slug, 날짜, 요약, 태그)
│   └── *.md            # 실제 글 마크다운 파일
└── CLAUDE.md
```

## 동작 방식(의도한 흐름)

1. 글은 `posts/` 안의 `.md` 파일로 작성한다.
2. `posts/posts.json`이 글 메타데이터(제목, slug, 날짜, 요약, 태그)의 인덱스 역할을 한다. 새 글을 추가하면 이 인덱스에도 항목을 넣는다.
3. 홈(`index.html`)은 `posts.json`을 `fetch`로 읽어 최신순 목록을 렌더링한다.
4. 글 링크는 `post.html?slug=글-slug` 형태다. `post.html`은 해당 `.md`를 `fetch`로 불러와 마크다운→HTML로 변환해 보여준다.
5. 다크모드 상태는 페이지 로드 시 가장 먼저 적용해 깜빡임(FOUC)을 피한다.

## 코딩 규칙

- **바닐라 JS**: `fetch` + `async/await`, ES 모듈 또는 명확한 함수 단위로 작성합니다. 전역 오염을 피합니다.
- **CSS 변수 우선**: 색상·간격·폰트 크기 등은 `:root`의 커스텀 프로퍼티로 정의하고 재사용합니다. 하드코딩된 색상값을 여러 곳에 반복하지 않습니다.
- **인라인 스타일/스크립트 지양**: 다크모드 초기 적용 스크립트처럼 꼭 필요한 경우를 제외하고 로직은 파일로 분리합니다.
- **점진적 향상**: JS가 실패하더라도 기본 콘텐츠와 링크는 접근 가능하도록 지향합니다.
- **`fetch` 경로**는 `file://`가 아니라 로컬 서버 기준으로 동작함에 유의합니다(아래 실행 방법 참고).

## 실행 / 미리보기

빌드 단계가 없으므로 정적 서버만 있으면 됩니다. `fetch`가 `file://`에서 막히므로 반드시 로컬 서버로 확인합니다.

```bash
# 프로젝트 루트에서
python3 -m http.server 8000
# 그다음 http://localhost:8000 접속
```

## 새 글 추가 절차

1. `posts/새-글-slug.md` 파일 작성.
2. `posts/posts.json`에 항목 추가:
   ```json
   { "slug": "새-글-slug", "title": "제목", "date": "2026-07-11", "summary": "한 줄 요약", "tags": ["태그"] }
   ```
3. 로컬 서버로 홈과 글 페이지가 잘 나오는지 확인.

## 작업 시 유의사항

- 새 기능을 추가할 때 프레임워크·번들러·npm 의존성을 끌어들이지 않습니다. 방향이 바뀌어야 한다면 먼저 사용자에게 확인합니다.
- 스타일 변경 후에는 **라이트/다크 두 모드**와 **모바일/데스크톱 두 폭**에서 모두 확인합니다.
- 마크다운 렌더링 시 사용자 콘텐츠를 삽입하므로 XSS에 주의합니다(신뢰 가능한 자체 글만 다루더라도 sanitize 옵션을 고려).
